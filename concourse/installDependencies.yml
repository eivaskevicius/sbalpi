---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: node
    tag: latest

inputs:
  - name: resource-sbalpi
outputs:
  - name: resource-sbalpi-installed

run:
  path: sh
  args: [resource-sbalpi/src/scripts/nodeDependencies.sh]