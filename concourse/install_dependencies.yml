---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: node
    tag: latest

inputs:
  - name: resource-sbalpi

run:
  path: resource-sbalpi/src/scripts/nodeDependencies.sh